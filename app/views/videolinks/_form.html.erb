<%= form_for(@videolink) do |f| %>
  <% if @videolink.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@videolink.errors.count, "error") %> prohibited this videolink from being saved:</h2>

      <ul>
      <% @videolink.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :link %><br>
    <%= f.text_field :link %>
  </div>
  <div class="field">
    <%= f.label :User_id %><br>
    <%= f.text_field :User_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
